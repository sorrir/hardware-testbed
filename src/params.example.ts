export const params = {
    mqttURL:                "mqtt://",      // URL of your MQTT broker
    mqttUser:               "",                     // username and password for MQTT broker if needed
    mqttPw:                 "",     // otherwise leave empty

    tickRateInMilliSec:     500,                        // tick rate (or time between execution of state machine state) in
// milliseconds
}